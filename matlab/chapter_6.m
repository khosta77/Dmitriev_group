%% 1
% Вычислить интеграл
a0 = @(x) cos(x - sqrt(2)) .* exp(2 .* sin(x));
a = integral(a0, 0, 4);

%% 2
% Вычислить интеграл 2
b0 = @(x) sin(x) - x.^2 .* cos(x);
b = integral(a0, -5, 0);

%% 3
% найти корни уравнения
c0 = @(x) x .* tan(x) .^2;
c = integral(c0, 0, 0.742);

%% 4
% ???